# 项目状态

更新时间：2026-07-24

## 当前路线

Office.js 个人侧载在当前世纪互联租户中不可用，因此正式路线已经切换为
classic Outlook 用户级 COM 加载项。

## 已完成

- 可维护的 C# 分层项目结构。
- Outlook COM 入口和自定义右侧任务窗格。
- Outlook 功能区“显示助手”开关。
- 按钮触发的当前邮件与同一 Outlook 会话历史读取。
- 会话邮件跨文件夹读取、时间排序、EntryID 去重和 50 封上限取样。
- 会话背景、关键时间线和当前邮件分层摘要提示词。
- 历史邮件重复引用清理与总字符预算。
- DeepSeek、OpenAI、豆包和自定义兼容 API 配置。
- Windows DPAPI API Key 加密。
- 自然语言转受限 JSON 搜索计划。
- JSON 校验、字段白名单、值转义和 AQS 编译。
- Outlook `Explorer.Search` 本地搜索调用。
- 用户级安装和卸载脚本。
- 无第三方依赖的构建与单元测试。

## 自动验证结果

- C# Release 编译：通过。
- COM 元数据 / RegAsm 解析：通过。
- 单元测试：11/11 通过。

## 真实 Outlook 验收结果

- 用户级安装与 COM 注册：通过。
- classic Outlook 首次加载：通过。
- 右侧任务窗格停靠：通过。
- Home 功能区“显示助手”按钮：通过。
- 侧栏显示 / 隐藏切换：通过。
- API 设置窗口首次打开与提供商列表初始化：通过。
- “生成会话背景与当前邮件摘要”按钮和新版隐私说明加载：通过。
- 未选择邮件时保持“尚未读取邮件”且不自动发送请求：通过。

## 尚需用户 API Key 验收

- 使用一个测试 API Key 验证真实会话背景与当前邮件摘要请求。
- 使用当前 Outlook 索引验证 AQS 日期、附件和中文关键词搜索。
- 确认关闭 Outlook 后更新与卸载流程。

以上测试涉及用户自己的 API Key、真实邮件或卸载操作，因此未在自动验收中执行。

## 历史原型

根目录的 Office.js 清单、TypeScript 任务窗格和本地 HTTPS 工具保留为历史
参考，不参与桌面版发布。
