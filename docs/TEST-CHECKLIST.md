# 测试清单

## 自动构建

- [x] 编译 Release DLL。
- [x] 解析 DeepSeek / OpenAI / 豆包兼容 URL。
- [x] 解析 snake_case 搜索计划 JSON。
- [x] 编译人员、关键词、日期和附件 AQS。
- [x] 清理模型生成值中的引号和括号。
- [x] 拒绝空搜索计划。
- [x] 使用 RegAsm 读取 COM 类型和依赖。
- [x] 清理独立历史邮件中的 Outlook 引用头。
- [x] 保留当前邮件中只存在于引用里的外部历史。
- [x] 构建分离的“历史会话 / 当前邮件”提示词。

## Outlook 首次安装

- [x] 完全退出 Outlook。
- [x] 等效执行 `Install.cmd` 的用户级安装流程，未使用管理员权限。
- [x] 启动 classic Outlook。
- [x] 确认右侧出现“AI 邮件助手”。
- [x] 确认 Home 功能区出现“显示助手”。
- [x] 切换显示/隐藏侧栏。
- [x] 确认侧栏显示“生成会话背景与当前邮件摘要”和新版隐私说明。

## 设置

- [x] 首次打开设置窗口，提供商列表正常初始化且不抛出异常。
- [ ] 选择 DeepSeek 并保存 API Key。
- [ ] 重启 Outlook 后确认设置仍在。
- [ ] 确认设置 JSON 中不存在明文 API Key。
- [ ] 切换 OpenAI、自定义兼容 API。
- [ ] 豆包模型 / 接入点 ID 为空时阻止保存。

## 手动摘要

- [ ] 未选中邮件时点击摘要，出现明确提示。
- [ ] 选中阅读窗格邮件，点击后才开始读取同一会话并请求。
- [ ] 单独打开邮件窗口，点击后能摘要该邮件及其历史会话。
- [ ] 侧栏显示独立历史邮件数和 Outlook 会话项目数。
- [ ] 输出包含会话背景、关键时间线、既有决定和当前状态。
- [ ] 当前邮件部分与历史背景分开，引用内容不会被误判为新要求。
- [ ] 摘要包含与当前用户相关的行动项和截止时间。
- [ ] 不点击按钮时不产生任何 AI 请求。
- [ ] 超长会话显示取样或字符截断提示。
- [ ] 复制摘要按钮可用。

## 智能搜索

- [ ] 输入“Richard 上个月带附件的 Malaysia 报价邮件”。
- [ ] 侧栏显示结构化条件和最终 AQS。
- [ ] Outlook 主窗口显示本地搜索结果。
- [ ] 日期范围符合当前日期。
- [ ] 中文关键词、英文关键词和发件人条件有效。
- [ ] “当前文件夹”描述只搜索当前文件夹。
- [ ] 断网时显示连接错误且不影响 Outlook。
- [ ] 确认搜索调用期间没有读取任何 MailItem。

## 更新与卸载

- [ ] Outlook 运行时安装/卸载会被阻止。
- [ ] 关闭 Outlook 后可覆盖安装新版本。
- [ ] 更新后设置和加密 API Key 保留。
- [ ] `Uninstall.cmd` 删除加载项注册。
- [ ] 默认卸载保留设置。
- [ ] 使用 `uninstall.ps1 -RemoveSettings` 可选择删除全部本地数据。
